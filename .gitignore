node_modules/
dist/
*.log

# Credentials. .env.example is the only one that ships.
.env
.env.*
!.env.example

# Provider private keys (REVENUE_MONSTER_PRIVATE_KEY_PATH points in here).
secrets/
*.pem
*.key
